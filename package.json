{
  "name": "pet_cypress_project",
  "version": "2.0.0",
  "description": "pet_cypress_project",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "start": "npx cypress run",
    "test": "cypress run",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:record": "cypress run --record --key"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.13.3",
    "cypress-xpath": "^2.0.1",
    "start-server-and-test": "^2.0.3"
  },
  "dependencies": {
    "faker": "^6.6.6"
  }
}
